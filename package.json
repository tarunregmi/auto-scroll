{
  "name": "auto-scroll",
  "version": "0.0.0",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/tarunregmi/auto-scroll.git"
  },
  "author": "Tarun Regmi",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -r icons manifest.json ./dist"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
